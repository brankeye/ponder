import React from 'react';
import { Button } from 'components/presenters';

const FavoriteButton = props => {
  return <Button title="Favorite" onPress={() => null} />;
};

export default FavoriteButton;
