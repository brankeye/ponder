query(
  $from: AuthorCategory!
  $first: Int
  $after: String
  $last: Int
  $before: String
) {
  authorList(
    from: $from
    first: $first
    after: $after
    last: $last
    before: $before
  ) @connection(key: "authorList", filter: ["from"]) {
    edges {
      node {
        id
        name
        inLibrary
        poems {
          id
          title
          teaser
          lines
          inLibrary
        }
      }
    }
    pageInfo {
      endCursor
    }
  }
}
