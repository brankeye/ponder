query PoemList($first: Int) {
  poemList(first: $first) {
    edges {
      node {
        id
        title
        teaser
        isFavorited
        isBookmarked
        author {
          name
        }
      }
    }
  }
}
