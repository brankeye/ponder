extend type Query {
  author(id: ID): Author
  authorList(limit: Int = 20, offset: Int = 0): [Author]
}

type Author {
  id: ID
  name: String
  isFavorited: Boolean
  isBookmarked: Boolean
  poems: [Poem]
}

input AuthorInput {
  name: String!
}
