extend type Query {
  poem(id: ID): Poem
}

extend type Mutation {
  poemCreate(input: PoemInput): Poem
  poemUpdate(id: ID, input: PoemInput): Boolean
}

type Poem {
  id: ID
  title: String
  teaser: String
  isFavorite: Boolean
  inLibrary: Boolean
  author: Author
}

input PoemInput {
  title: String!
  teaser: String!
  authorId: ID!
}
