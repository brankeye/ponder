pipeline:
  deploy:
    image: docker/compose
    commands:
      - "set -a && source /opt/ponder/.dev.env && set +a"
      - "docker-compose build"
      - "docker-compose up -d"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /opt/ponder/.dev.env:/opt/ponder/.dev.env
branches:
  include: [dev, stg, prd]
